{
  "SIG-01": ["produces a valid v1 signature"],
  "SIG-02": ["rejects tampered payload — different signature"],
  "SIG-03": ["rejects wrong secret — different signature"],
  "SIG-04": ["is deterministic — same inputs produce same signature"],
  "SIG-05": ["rejects empty or missing secret"],
  "SIG-06": ["rejects malformed signature header"],

  "CLI-01": ["rejects invalid API key prefix"],
  "CLI-02": ["accepts valid API key"],
  "CLI-03": ["rejects invalid token prefix"],
  "CLI-04": ["accepts valid management token"],
  "CLI-05": ["baseUrl config option overrides region-based URL"],

  "ING-01": ["send() calls correct endpoint"],
  "ING-02": ["send() calls correct endpoint"],
  "ING-03": ["trigger() calls correct endpoint"],
  "ING-04": ["trigger() calls correct endpoint"],

  "MGT-EP-01": ["list() sends GET to /endpoints"],
  "MGT-EP-02": ["create() sends POST with body"],
  "MGT-EP-03": ["get() sends GET to /endpoints/:id"],
  "MGT-EP-04": ["update() sends PATCH with body"],
  "MGT-EP-05": ["delete() sends DELETE"],

  "MGT-ET-01": ["list() sends GET to /event-types"],
  "MGT-ET-02": ["create() sends POST with body"],
  "MGT-ET-03": ["get() sends GET to /event-types/:id"],
  "MGT-ET-04": ["update() sends PATCH with body"],
  "MGT-ET-05": ["delete() sends DELETE"],

  "MGT-APP-01": ["list() omits undefined query params"],
  "MGT-APP-02": ["list() sends GET with limit/offset query params"],
  "MGT-APP-03": ["create() sends POST with body"],
  "MGT-APP-04": ["get() sends GET to /applications/:id"],
  "MGT-APP-05": ["update() sends PATCH"],
  "MGT-APP-06": ["delete() sends DELETE"],
  "MGT-APP-07": ["listEndpoints() sends GET to /applications/:id/endpoints"],
  "MGT-APP-08": ["createEndpoint() sends POST to /applications/:id/endpoints"],

  "MGT-SUB-01": ["list() sends GET to /endpoints/:id/subscriptions"],
  "MGT-SUB-02": ["create() sends POST with eventTypeId"],
  "MGT-SUB-03": ["delete() sends DELETE with eventTypeId in path"],

  "MGT-PS-01": ["create() sends empty body when no options"],
  "MGT-PS-02": ["create() sends POST to /applications/:id/portal"],

  "MGT-ENV-01": ["list() sends GET to /environments"],
  "MGT-ENV-02": ["create() sends POST with body"],
  "MGT-ENV-03": ["get() sends GET to /environments/:id"],
  "MGT-ENV-04": ["update() sends PATCH with body"],
  "MGT-ENV-05": ["delete() sends DELETE"],
  "MGT-ENV-06": ["listEventTypeVisibility() sends GET to /environments/:id/event-types"],
  "MGT-ENV-07": ["setEventTypeVisibility() sends PUT with body"],

  "ERR-01": ["identifies retryable errors"],
  "ERR-02": ["identifies retryable errors"],
  "ERR-03": ["identifies retryable errors"],
  "ERR-04": ["identifies auth errors"],
  "ERR-05": ["identifies auth errors"],
  "ERR-06": ["identifies auth errors"],
  "ERR-07": ["exposes convenience getters"],
  "ERR-08": ["exposes convenience getters"],
  "ERR-09": ["exposes convenience getters"],
  "ERR-10": ["wraps the original error"],
  "ERR-11": ["stores timeout duration"],

  "RET-01": ["returns a value between 0 and exponential cap"],
  "RET-02": ["caps at MAX_DELAY_MS"],
  "RET-03": ["returns retryAfterMs when provided"],

  "REG-01": ["resolves US base URL from nhk_us_xxx key"],
  "REG-02": ["resolves EU base URL from nhk_eu_xxx key"],
  "REG-03": ["resolves AP base URL from nhk_ap_xxx key"],
  "REG-04": ["falls back to default URL for unknown region"],
  "REG-05": ["baseUrl config option overrides region-based URL"],

  "HDR-01": ["sends Authorization header with management token"],
  "HDR-02": ["sends User-Agent header"],
  "HDR-03": ["includes Content-Type on POST requests"],
  "HDR-04": ["omits Content-Type on GET requests"],

  "NEG-01": ["NEG-01: malformed JSON response throws"],
  "NEG-02": ["NEG-02: empty body on 200 throws"],
  "NEG-03": ["NEG-03: 5xx HTML body throws NahookAPIError with isRetryable"],
  "NEG-04": ["NEG-04: 5xx empty body throws NahookAPIError with isRetryable"],
  "NEG-05": ["NEG-05: extra unknown fields in response — succeeds without crashing"],
  "NEG-06": ["NEG-06: minimal fields in response — succeeds without crashing"],

  "PBT-01": ["sign then verify always succeeds"],
  "PBT-02": ["tampered payload always fails verification"],
  "PBT-03": ["wrong secret always fails verification"],
  "PBT-04": ["sign is deterministic — same input, same output"]
}
